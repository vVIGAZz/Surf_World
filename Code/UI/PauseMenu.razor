@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent
@namespace Sandbox

<root>
    <div class="content">
        <div class="text">Press B to back Main Menu</div>
    </div>
    <div class="content">
        <div class="text">Press R to Respawn</div>
    </div>

</root>

@code
{


    [Property] private SceneFile mainMenu;
    protected override void OnUpdate( )
    {
        BackToMenu();
    }

    private void BackToMenu()
    {
        if (Input.Keyboard.Pressed("B"))
        {
            Scene.Load(mainMenu);
        }
    }
    protected override int BuildHash() => System.HashCode.Combine(BackToMenu);
}
